[pytest]
# Sin esto, los tests declarados con `async def` no se ejecutan: pytest los
# salta con el aviso "async def functions are not natively supported".
# El modo automatico evita tener que poner @pytest.mark.asyncio en cada uno.
asyncio_mode = auto

testpaths = tests
